# Local development only. Never commit a real .env.
# In AWS these come from the task definition; secrets come from Secrets Manager.
APP_ENV=local
LOG_LEVEL=DEBUG
PORT=8000
APP_VERSION=0.0.0-dev
GIT_SHA=local
BUILD_NUMBER=0
